# The embedded-postgres binary subpackages (@embedded-postgres/<platform>) are
# published only to public npm, not the default registry. Pin that scope to npm
# so installs (including --frozen-lockfile) resolve them from the public registry.
# All other packages keep using the default registry from the user/global .npmrc.
@embedded-postgres:registry=https://registry.npmjs.org/
